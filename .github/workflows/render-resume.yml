name: Render resume HTML

on:
  push:
    branches:
      - main
    paths:
      - README.md
      - README.pt-BR.md
      - scripts/generate-resume.js
      - .github/workflows/render-resume.yml
      - felipe-avatar.jpg
  workflow_dispatch:

permissions:
  contents: write

concurrency:
  group: render-resume-html
  cancel-in-progress: false

jobs:
  render:
    runs-on: ubuntu-latest

    steps:
      - name: Check out repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "22"

      - name: Generate HTML from README
        run: node scripts/generate-resume.js

      - name: Commit regenerated output
        run: |
          artifacts="index.html en.md pt.md llms.txt"

          if git diff --quiet -- $artifacts; then
            echo "generated output is already up to date"
            exit 0
          fi

          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add $artifacts
          git commit -m "chore: regenerate resume output [skip ci]"
          git push
